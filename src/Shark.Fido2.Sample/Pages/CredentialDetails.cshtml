@page
@model Shark.Fido2.Sample.Pages.CredentialsDetailsModel

<div class="main">
    <h1 class="display-4 mb-4 text-center">Credential Details</h1>

    <div class="main-controls-container">
        @if (Model.CredentialId == null || Model.CredentialId.Length == 0)
        {
            <div id="credentials-details-container" class="section p-4">
                <div class="alert alert-warning">
                    No credential details available. Please sign in first.
                </div>
            </div>
        }
        else
        {
            <div id="credentials-details-container" class="section p-4 border rounded">
                <div class="card-body">
                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Credential identifier</strong>
                        </div>
                        <div class="col-md-8">
                            <code>@Convert.ToBase64String(Model.CredentialId!)</code>
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>User handle</strong>
                        </div>
                        <div class="col-md-8">
                            <code>@Convert.ToBase64String(Model.UserHandle)</code>
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Username</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.UserName
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Display name</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.UserDisplayName
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Sign count</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.SignCount
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Algorithm</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.Algorithm
                        </div>
                    </div>

                    @if (Model.Transports != null && Model.Transports.Any())
                    {
                        <div class="row mb-4">
                            <div class="col-md-4">
                                <strong>Transports</strong>
                            </div>
                            <div class="col-md-8">
                                <ul class="list-unstyled mb-0">
                                    @foreach (var transport in Model.Transports)
                                    {
                                        <li>@transport</li>
                                    }
                                </ul>
                            </div>
                        </div>
                    }

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Time of creation (UTC)</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.CreatedAt
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Time of last update (UTC)</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.UpdatedAt
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-4">
                            <strong>Time of last use (UTC)</strong>
                        </div>
                        <div class="col-md-8">
                            @Model.LastUsedAt
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>